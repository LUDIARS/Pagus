{
  "name": "@pagus/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ludiars/pictor-browser": "file:ludiars-pictor-browser-0.1.0.tgz",
    "@pagus/sim": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.0.0"
  }
}
